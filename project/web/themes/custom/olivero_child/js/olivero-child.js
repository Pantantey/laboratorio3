/**
 * @file
 * olivero-child behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.oliveroChild = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
